#include<stdio.h>
void main()
{
int first,terms,diff,i,sum=0;value;
printf("enter the first term,no of terms,differnce:\t");
scanf("%d%d%d",&first,&terms,&diff);
value=first;
printf("AP series is:\t");
for(i=0;1<n;i++);
{
printf("%d",value);
sum+=value;
value=value+diff;
}
printf("Sum of AP series of %d is %d",term,sum);
}
